specVersion: "v1"
serviceId: "flexitask"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "flexitask-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
